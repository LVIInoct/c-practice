Stack implementation. The reason why it lacks comments is because I found out I can just reutilize
the queue code and convert it into a stack. It works almost the same, it's mostly just inventory
management difference (Last In, First Out).
